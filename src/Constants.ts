import * as THREE from 'three';

const Constants = {
  selectedBody: "0-2",
  timeMultipleIndex: 14,
  startingRelativePosition: new THREE.Vector3(3300, 0, -1),
  startDate: new Date(),
  cameraNear: 0.0005,
};

export default Constants;